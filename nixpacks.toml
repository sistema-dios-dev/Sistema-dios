[phases.setup]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python railway_start.py"

[phases.build]
cmds = ["echo '🚀 Building Sistema Dios...'"]

[providers]
railway = true
render = true
